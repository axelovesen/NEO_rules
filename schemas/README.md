# Schemas

Machine-readable conversion contracts are maintained in `../contracts/`.
This directory is retained for future schema-specific documentation.

**NEEDS_NEO_VALIDATION:** No additional schema contracts are defined here.
