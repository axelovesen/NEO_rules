# Rules

Authoritative semantic mapping, value, calculation, address, ambiguity, and
quality-gate rules are in this directory. Read `neo-fields.md` first.

Unsupported template details and business requirements remain explicitly marked
`TODO` or `NEEDS_NEO_VALIDATION`.
