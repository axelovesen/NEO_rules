# Mappings

Mappings are intentionally produced per workbook as a reusable mapping plan;
this repository does not contain a hard-coded broker-header dictionary.
Validate plans against `../contracts/mapping-plan.schema.json`.
